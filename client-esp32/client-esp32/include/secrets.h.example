#pragma once

// Copy this file to secrets.h and fill in real values. secrets.h is
// gitignored so your actual WiFi/broker credentials never get committed.

const char* ssid = "YOUR_WIFI_SSID";
const char* password = "YOUR_WIFI_PASSWORD";

const char* mqttBroker = "YOUR_BROKER_IP"; // e.g. your laptop's LAN IP running backend/main.py
const char* serverUrl = "http://YOUR_BROKER_IP:8000/upload"; // only used if ENABLE_CAMERA is on
